{
    "name": "Snowflake development tools",
    "id": "snowflake",
    "version": "1.0.8",
    "description": "Installs Snowflake development tools.",
    "options": {
      "installSnowSQL": {
        "type": "boolean",
        "default": true,
        "description": "Installs the SnowSQL CLI."
      },
      "snowSQLVersion": {
        "type": "string",
        "default": "1.4.5",
        "description": "SnowSQL version"
      },
      "installSnowflakeCLI": {
        "type": "boolean",
        "default": true,
        "description": "Installs the Snowflake CLI."
      },
      "snowflakeCLIVersion": {
        "type": "string",
        "default": "3.14.0",
        "description": "Snowflake CLI version. The deb package is installed when the version number is used. The pipx value installs the latest version using pipx."
      }
    },
    "containerEnv": {
        "SF_AUTH_SOCKET_PORT": "53154",
        "SF_AUTH_SOCKET_REUSEPORT": "true"
    },
    "customizations": {
        "vscode": {
          "extensions": [
            "snowflake.snowflake-vsc"
          ]
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
